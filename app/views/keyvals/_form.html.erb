<%= form_for(@keyval) do |f| %>
  <% if @keyval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyval.errors.count, "error") %> prohibited this keyval from being saved:</h2>

      <ul>
      <% @keyval.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br>
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
